class DemoLLM(LLM):
    async def chat(self, *args, **kwargs):
        text = (
            "I am explaining something important. "
            "This explanation should continue even if you say yeah or okay. "
            "Please listen carefully."
        )
        for word in text.split():
            yield word + " "
            await asyncio.sleep(0.import asyncio
from livekit.agents import Agent, AgentServer, AgentSession, JobContext, cli
from livekit.agents.llm import LLM

class DemoLLM(LLM):
    async def generate(self, *args, **kwargs):
        text = (
            "I am explaining something important. "
            "This explanation should continue even if you say yeah or okay. "
            "Please listen carefully."
        )
        for word in text.split():
            yield word + " "
            await asyncio.sleep(0.3)

class DemoAgent(Agent):
    def __init__(self):
        super().__init__(
            instructions="You are a demo agent explaining something important."
        )

server = AgentServer()

@server.rtc_session()
async def entrypoint(ctx: JobContext):
    session = AgentSession(
        llm=DemoLLM(),  # NO OpenAI here
    )

    await session.start(
        agent=DemoAgent(),
        room=ctx.room,
    )

if __name__ == "__main__":
    cli.run_app(server)
