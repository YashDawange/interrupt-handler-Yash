# LiveKit Agents with Intelligent Interruption Handling
# Required dependencies for the demo agent

# Core LiveKit Agents package
livekit-agents[openai,silero,deepgram]~=1.0

# For environment variable loading
python-dotenv>=1.0.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
