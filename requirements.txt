# Requirements for LiveKit Intelligent Interruption Agent
# 
# This project is a workspace with local packages. Install as follows:
#
# Option 1: Using pip with editable installs (recommended)
#   pip install -e ./livekit-agents
#   pip install -e ./livekit-plugins/livekit-plugins-openai
#   pip install -e ./livekit-plugins/livekit-plugins-silero
#   pip install -r requirements.txt
#
# Option 2: Using uv (if available)
#   uv sync --extra openai --extra silero
#
# Note: You need Python 3.9+ and an OPENAI_API_KEY in your .env file

# Core dependencies
python-dotenv>=1.0.1

# OpenTelemetry (pin to 1.34.1 for compatibility)
opentelemetry-api==1.34.1
opentelemetry-sdk==1.34.1
opentelemetry-exporter-otlp==1.34.1

# Audio/Video processing
numpy>=1.26.0
sounddevice>=0.5
av>=14.0.0,<16

# ONNX runtime for Silero VAD
onnxruntime>=1.18,<=1.23.1

# OpenAI SDK
openai[realtime]>=1.107.2

# LiveKit SDK
livekit>=1.0.19,<2
livekit-api>=1.0.7,<2
livekit-protocol>=1.1,<2

# Other dependencies
typer>=0.15.1
click~=8.1
aiohttp~=3.10
pydantic>=2.0,<3
nest-asyncio>=1.6.0
aiofiles>=24
psutil>=7.0
colorama>=0.4.6
pyjwt>=2.0
protobuf>=3
certifi>=2025.6.15
watchfiles>=1.0
docstring_parser>=0.16
typing-extensions>=4.12
eval-type-backport
prometheus-client>=0.22
pillow>=10.3.0

