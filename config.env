# Smart Interruption Agent Configuration
# ======================================
# Copy this file to .env and customize as needed

# LiveKit Connection (Required)
# -----------------------------
LIVEKIT_URL=wss://your-livekit-server.livekit.cloud
LIVEKIT_API_KEY=your-api-key-here
LIVEKIT_API_SECRET=your-api-secret-here

# API Keys (Required)
# ------------------
DEEPGRAM_API_KEY=your-deepgram-key-here
OPENAI_API_KEY=your-openai-key-here

# Interruption Behavior Configuration (Optional)
# ----------------------------------------------

# Filler Words (comma-separated, no spaces)
# These words will be IGNORED when the agent is speaking
# Default: yeah,ok,okay,hmm,uh-huh,mhmm,right,aha,yep,yup,sure,gotcha,alright,cool,nice,good,yes
FILLER_WORDS=yeah,ok,okay,hmm,uh-huh,mhmm,right,aha,yep,sure,gotcha,alright

# Command Words (comma-separated, no spaces)
# These words will ALWAYS interrupt the agent
# Default: wait,stop,no,hold,pause,hang on,hold on,actually,but,however
COMMAND_WORDS=wait,stop,no,hold,pause,hang on,actually,but

# STT Wait Timeout (seconds)
# How long to wait for speech-to-text confirmation
# Lower = faster but might miss transcription
# Higher = slower but more accurate
# Recommended: 0.10 to 0.20 seconds
# Default: 0.15
STT_TIMEOUT=0.15

# Minimum Confidence Threshold (0.0 to 1.0)
# Only trust STT results above this confidence level
# Lower = trust partial/uncertain transcriptions
# Higher = only trust high-confidence transcriptions
# Default: 0.6
MIN_CONFIDENCE=0.6

# Logging Level (DEBUG, INFO, WARNING, ERROR)
# Default: INFO
LOG_LEVEL=INFO


# Example Configurations for Different Use Cases
# ===============================================

# Customer Support (More Permissive)
# ----------------------------------
# FILLER_WORDS=yeah,ok,okay,hmm,uh-huh,mhmm,right,aha,yep,sure,gotcha,understood,alright,cool,nice,good,great,perfect
# COMMAND_WORDS=wait,stop,no,hold,pause,hang on,actually,but,however,question,sorry,excuse me
# STT_TIMEOUT=0.20

# Voice Assistant (Balanced)
# --------------------------
# FILLER_WORDS=yeah,ok,hmm,uh-huh,yep
# COMMAND_WORDS=stop,wait,pause,no,cancel
# STT_TIMEOUT=0.15

# Educational/Tutorial (Very Permissive)
# --------------------------------------
# FILLER_WORDS=yeah,ok,okay,hmm,uh-huh,oh,wow,cool,nice,interesting,right,gotcha,makes sense
# COMMAND_WORDS=wait,question,but,what,why,how,confused,unclear,can you
# STT_TIMEOUT=0.20

# Professional/Business (Conservative)
# ------------------------------------
# FILLER_WORDS=yes,understood,agreed,noted
# COMMAND_WORDS=wait,stop,hold,pause,question,clarification,point of order
# STT_TIMEOUT=0.10

# Quick Response Mode (Fast but Less Accurate)
# --------------------------------------------
# STT_TIMEOUT=0.10
# MIN_CONFIDENCE=0.5

# Accuracy Mode (Slower but More Reliable)
# ----------------------------------------
# STT_TIMEOUT=0.20
# MIN_CONFIDENCE=0.7
