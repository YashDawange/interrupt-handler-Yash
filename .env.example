LIVEKIT_URL="wss://your-project.livekit.cloud"
LIVEKIT_API_KEY="your-api-key"
LIVEKIT_API_SECRET="your-api-secret"

ANTHROPIC_API_KEY="your-anthropic-key"
DEEPGRAM_API_KEY="your-deepgram-key"

# words that should be ignored ONLY when the agent is speaking
AGENT_PASSIVE_LEXICON="yeah,yes,yep,yup,uhhuh,ok,okay,k,mhmm,uh-huh,mhm,mmhmm,hmm,right,sure,gotcha,i see"

# words that should always trigger an immediate interruption
AGENT_DIRECTIVE_COMMANDS="stop,wait,pause,no,actually,but,hold,cancel"